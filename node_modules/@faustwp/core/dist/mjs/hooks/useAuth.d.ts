declare type RedirectStrategyConfig = {
    strategy: 'redirect';
    shouldRedirect?: boolean;
};
declare type LocalStrategyConfig = {
    strategy: 'local';
    loginPageUrl: string;
    shouldRedirect?: boolean;
};
export declare type UseAuthConfig = RedirectStrategyConfig | LocalStrategyConfig;
export declare function useAuth(_config?: UseAuthConfig): {
    isAuthenticated: boolean | null;
    isReady: boolean;
    loginUrl: string | null;
};
export {};
